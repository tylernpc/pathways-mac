namespace abstraction;

class Program
{
    static void Main(string[] args)
    {

        // creating the list of employees
        List<Person> employeeList = new List<Person>();
        
        

        /*
        employeeList.Add(new SalaryEmployee(90000, "Le", "Tyler"));
        employeeList.Add(new HourlyEmployee(1, "Bolt", "Adam"));
        employeeList.Add(new HourlyEmployee(1, "Bolt", "Adam"));

        var foundEmployees = employeeList.Where(x => x.Type == "Hourly");

        foreach (Employee anEmployee in foundEmployees)
        {
            Console.WriteLine(anEmployee);
        }
        */
    }
}