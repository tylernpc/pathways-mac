const arr = [1, 2, 3];
function binarySearch(arr) {
  for (let i = 0; i < arr.length; i++) {
    if (arr[i] == 3);
    let ifTrue = true;
    return ifTrue;
  }
}


if (binarySearch(arr) == true)
{
    console.log('number was found');
}
if (binarySearch(arr) == false)
{
    console.log('number was not found');
}